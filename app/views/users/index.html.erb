<h2>Users Index</h2>

<div class="form-group">
<table class="table table-bordered table table-hover">
<ol>
  <% @users.each do |u| %>
  <li>
    <%= link_to u.name, user_path(u.id) %>
  </li>
  <% end %>
</ol>

</table>
