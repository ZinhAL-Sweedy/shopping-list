<h2>Edit Catogery</h2>
<!-- <form action="/categories/<%= @category.id %>" method="post"> -->
Name:
<input type="text" name="name" value="<%= @category.name %>">
<br>

<%= form_for @category do |f| %>
<p>List of Products



  <div>
    <%= f.label "products", class:"bg-info" %>
    <%= f.collection_check_boxes :product_ids, Product.all, :id, :name, id:"blankCheckbox", value:"option1" do |p| %>

    <div>
      <%= p.check_box (class:"text-primary") %>
      <%= p.label %>
    </div>
    <% end %>
  </div>
</p>

<%= f.submit (class: "btn btn-success") %>
<% end %>
